spring:
  data:
    mongodb:
      uri: mongodb://VENDA-MONGO-SERVER:27017/test
eureka:
  instance:
    instance-id: ${spring.application.name}:${spring.application.instance_id:${random.value}}
  client:
    service-url:
      defaultZone: http://EUREKA-SERVER:8761/eureka
server:
  port: 8080